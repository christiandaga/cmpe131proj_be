export const login = async (username, password) => {
    // compare password
    // get and return user
    const exampleuser = {
        id: 1,
        username,
        password,
        pin: 1234
    }
    return exampleuser;
}